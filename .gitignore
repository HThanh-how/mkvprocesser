__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.DS_Store
*.log
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Build outputs
build/
dist/
*.spec

# PyInstaller temp/dist
*.manifest
*.dll

# MKVProcessor executable (quá lớn, không commit)
dist/MKVProcessor.exe
dist/**/MKVProcessor.exe

# FFmpeg binaries — downloaded on demand (tools/download_ffmpeg.py /
# scripts/build_complete.py) and regenerated by build-release CI.
# Never commit (was ~190MB of Windows .exe). History purge of old blobs is a
# separate, destructive step — see ROADMAP.md Phase 2.
ffmpeg_bin/

# Local configs/secrets
config.json
auto_push_config.json
auto_push_config*.json

# Editor/workspace
.vscode/
.idea/
.cursor/

.env

# --- mkvtools runtime & secrets (Phase 1) ---
config.yaml
secrets/*
!secrets/.gitkeep
client_secret*.json
token.json
*.pem
*.key
inbox/*
!inbox/.gitkeep
work/
done/
*.egg-info/
